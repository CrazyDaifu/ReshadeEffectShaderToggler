# 项目移交：REST Battlefield 2 窗口模式修复

## 当前结论

Battlefield 2（2005）在原生 32 位 D3D9 窗口模式进入地图后，启用 REST 配置会让
ReShade 所有效果消失。全屏模式正常，使用 dgVoodoo2 转译到 D3D11 后窗口模式也正常。

最终根因是 ReShade 的 D3D9 后备缓冲描述为 `resource_type::surface`，而 REST 的
`GlobalResourceView` 只接受 `texture_2d`。REST 已把 ReShade 本帧效果标记为已处理，
但无法为后备缓冲建立 RTV，因而剩余效果没有输出。

有效修复只有 `src/GlobalResourceView.cpp` 的一处类型兼容：接受
`texture_2d || surface`。项目所有者已确认原生 D3D9 全屏和窗口模式功能正常。

## 已否定方案

- Present 时改用显式 swapchain 后备缓冲：不能解决类型过滤。
- 记录 shader bind 并在未观察到绑定时强制渲染全部 technique：成功日志证明该分支
  没有参与最终成功路径，而且会改变 REST 的效果分组语义。
- Reset 后立即重建无效缓存视图：原版已有延迟清理，立即重建增加资源生命周期风险。
- `render_effects(cmd, 0, 0)` 作为默认目标：ReShade 6.3.7 会先设置本帧已渲染标志，
  随后因 RTV 为空返回，不能用作后备缓冲选择机制。

## 已验证环境

- Battlefield 2 (2005)
- ReShade 6.3.7，完整 add-on 版本
- 原生 D3D9，32 位 `d3d9.dll`
- Release / x86
- Visual Studio Community 2026，MSVC 14.51 / v145
- Windows SDK 10.0.19041.0

## 版本关系

- 上游：`4lex4nder/ReshadeEffectShaderToggler`
- 上游提交：`6d63c9d1dfd3ab47d7ab1512ecb8111d2aa461a6`
- 上游资源版本：`1.3.22.633`
- 当前候选：`1.3.22.633-bf2-windowed.1`

## 维护约束

1. 不要恢复强制 Present 补渲染代码。
2. 不要用 dgVoodoo2 或 DXVK 作为原生 D3D9 修复的运行时依赖。
3. 每次上传前运行构建和 PE32/导出验证脚本。
4. 候选版本保留在 `releases/<VERSION>/`，不能被其他版本覆盖。
5. 正式发布前补测 Alt+Tab、多次换图、分辨率切换和窗口/全屏往返。
