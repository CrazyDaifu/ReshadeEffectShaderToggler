# Security policy

This add-on is loaded into the target game's process. Download binaries only from this
fork's GitHub Releases page or build them from the included source.

Crash or suspicious-behavior reports should include the game, graphics API, ReShade
version, GPU driver, add-on hash, and `ReShade.log`. The add-on does not intentionally
perform network access.
